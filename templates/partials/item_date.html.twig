<time class="dt-published is-size-7" datetime="{{ page.date|date("c") }}">
    <i class="fa fa-calendar" aria-hidden="true"></i>
    {{ page.date|localizeddate('long', 'none', grav.language.getActive) }}
</time>