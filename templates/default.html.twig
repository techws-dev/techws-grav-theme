{% extends 'partials/base.html.twig' %}

{% block content %}

    {% include 'partials/header_image.html.twig' with {'display':'header-image'} %}

    <div class="container">
        <div class="box">
            <div class="content">
                {{ page.content }}
            </div>
        </div>
    </div>
    
{% endblock %}
